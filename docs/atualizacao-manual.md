# Atualização manual

- Por competência na UI (`/importacoes`)
- Reprocessar job (novo enqueue com `force`)
- Somente descoberta (`discover_only`)
- Refresh respeita `manual_field_overrides` e grava `source_observations`
